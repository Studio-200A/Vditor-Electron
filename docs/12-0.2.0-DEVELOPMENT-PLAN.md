# Vditor-Electron 0.2.0 开发任务计划

> 项目仓库：<https://github.com/Studio-200A/Vditor-Electron>  
> 当前开发基线：`dev-0.2.0` 分支，HEAD 为 `548921f`；批次 6 已完成，当前工作区包含批次 6.5 的计划外主题扩展
> 当前开发基线：`0.1.5`（`package.json`）；`0.1.3` 是本计划最初建立时的发布基线
> 目标版本：`0.2.0`

> 状态说明：新版工作区 UI、查找替换、文件树基础操作、保存可靠性、恢复快照、完整文件 watcher 覆盖、modified/deleted/reappeared/unreadable 文件状态、工作区目录读取/监听深度边界及目录级路径一致性已实现并由 Tracker 记录；本文继续记录尚未完成的安全边界和发布验证。Claude 应用主题扩展不属于本计划原有功能目标，单独记录于批次 6.5。

## 1. 文档用途

本文档供 coding agent 在现有代码基础上继续开发 Vditor-Electron 0.2.0。它不是要求机械执行的静态命令清单，而是一份经过产品定位、现有架构和已知风险共同约束的开发计划。

开始编码前，agent 必须先检查当前仓库 HEAD、工作区状态、现有实现和测试结果。如果当前代码已经变化，或者某项任务的前提不再成立，应先报告差异并调整实施方案，不能为了逐字完成本文档而覆盖已有成果或引入重复实现。

## 2. 产品定位与开发边界

Vditor-Electron 的定位是一个本地优先、轻量、专注于打开和编辑普通 Markdown 文件的桌面应用。它基于 Vditor 编辑器引擎，但桌面文件生命周期、标签页、工作区、设置、主题、导出和系统集成由本项目负责。

0.2.0 必须继续遵守以下原则：

- 保持“Markdown 编辑器”定位，不扩展为知识库、数据库式笔记系统或云同步平台。
- 保持普通 `.md` / `.markdown` 文件作为真实数据源，不引入私有文档格式。
- 保持本地优先，不加入账号、遥测、在线服务或必须联网的功能。
- 保留三种 Vditor 编辑模式：所见即所得、即时渲染、分栏预览。
- 保留源码与预览的独立字体、字号、缩放和主题设置。
- 不在 0.2.0 升级 Vditor。继续固定当前 Vditor `3.11.3`，除非现有实现确实因上游缺陷无法完成任务，并在修改前说明理由。
- Vditor 私有 DOM 结构和选择器继续集中在 `src/renderer/vditor-adapter.js`，不得在其他模块随意新增散落的私有选择器。
- 不引入 React、Vue 等 UI 框架，不对渲染层进行一次性推倒重写。
- 不为了“代码看起来现代”而改变已经稳定的用户行为。

### 2.1 跨平台开发边界

0.2.0 至 0.2.5 的完整开发、E2E 和安装包冒烟验证以 Linux 为准；Windows 与 macOS 的实体机验证、安装包、签名和发布流程不属于本版本发布门槛。实现原则、Linux 可模拟范围、必须实机验证的项目和测试矩阵统一记录在 [`docs/03-CROSS-PLATFORM.md`](03-CROSS-PLATFORM.md)。任何未在对应平台实际执行过的验证都不得报告为已完成。

新增桌面能力的路径、平台边界和测试要求统一遵循 [`docs/03-CROSS-PLATFORM.md`](03-CROSS-PLATFORM.md)，不在本计划重复维护细节。

## 3. 0.2.0 版本目标

0.2.0 的显性主题是“更轻、更灵活的编辑界面”，底层目标是让应用具备更可靠的桌面文件编辑基础。当前代码已完成安全写入、恢复快照、工作区内外 watcher、外部修改冲突、外部删除/重新出现/不可读状态以及工作区目录读取/监听深度边界；目录级路径一致性和 Electron 安全边界仍未完成。跨平台实机验证按 `docs/03-CROSS-PLATFORM.md` 的后置计划执行。

本版本原计划应同时完成以下结果：

1. 提高文档保存、异常恢复和外部文件冲突处理的可靠性。
2. 收紧 Electron 的导航、IPC、外部链接和本地资源访问边界。
3. 修复已确认的 macOS 窗口关闭状态、目录重命名和删除后的标签页一致性问题。
4. 建立能够阻止明显回归的自动化检查和发布前验证流程。

### 3.1 计划外主题扩展边界

批次 6.5 在本计划建立后作为独立的用户体验需求加入。它只扩展应用壳层的颜色主题和亮暗主题偏好，不改变本计划的安全、文件可靠性、Vditor 版本或发布门槛。主题的详细架构、颜色、持久化字段、上游职责和验收记录见 [`docs/04-THEMES.md`](04-THEMES.md)。

## 4. 开始开发前的基线检查

在修改任何文件前完成以下检查，并把结果记录在开发汇报中：

- [ ] 确认当前分支、HEAD 和标签状态。
- [ ] 运行 `git status --short`，保留用户已有修改，不覆盖不属于本任务的变更。
- [ ] 阅读 `README.md`、`docs/20-VDITOR-UPGRADE.md`、`docs/00-ISSUES.md`。
- [ ] 检查 `src/main`（含 preload）、`src/renderer/app.js`、`src/renderer/vditor-adapter.js` 和现有测试。
- [ ] 运行现有的格式化检查、ESLint、TypeScript 类型检查、单元测试、构建和 E2E 测试。
- [ ] 如果 E2E 因容器或系统不允许 Chromium/Electron 创建单实例 socket 而无法启动，应记录为测试环境限制，不能把它误判为应用功能失败；仍需在兼容的本地环境完成 E2E。
- [ ] 根据当前代码确认本文档中的文件名、状态字段和任务前提是否仍然有效。

完成基线检查后再开始实施。若发现重大差异，应先提交一份简短的“计划调整说明”。

### 4.1 0.2.0 实施架构

0.2.0 采用面向本地 Markdown 编辑器的最小充分架构。实现范围以第 2 节的产品定位和本节的组件契约为准。

| 组件               | 技术契约                                                                                                           | 所有权与生命周期                                                  |
| ------------------ | ------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------- |
| 安全写入服务       | 接收目标路径和待写入字节，完成同目录临时写入、落盘、替换、权限保留和失败清理；成功后返回可供标签状态使用的预期正文 | main process 单一写入入口；不持有 watcher 或 UI 状态              |
| Recovery Store     | 按标签保存轻量 JSON 快照，启动时校验并提供恢复候选，保存或放弃后清理对应快照                                       | main process 持久化；renderer 只通过恢复流程所需的窄 IPC 使用     |
| File Watch Service | 管理工作区和独立文件监听，等待文件写入稳定后读取磁盘正文并发送规范化文件事件                                       | main process 持有 watcher；标签关闭、另存、工作区切换和退出时清理 |
| 标签文件状态       | 比较磁盘正文、本地正文和 `savedContent`，驱动自动重载、modified 冲突、deleted/reappeared/unreadable 状态与自动保存暂停 | renderer 按标签持有；文件事件是输入，不直接访问文件系统           |
| IPC 边界 helper    | 验证受信任主 frame；高风险 handler 在产生副作用前验证运行时参数                                                    | main process 统一复用；preload 只暴露产品需要的窄方法             |
| 本地资源受控根     | 维护当前工作区根和独立文档目录，规范化并验证本地资源协议请求                                                       | 随文档和工作区打开而注册、关闭而撤销                              |
| 导出服务           | 从一次性内容快照生成可独立打开的 HTML/PDF，转换本地图片来源并隔离 PDF 隐藏窗口                                     | 每次导出独立创建和释放所需资源，不继承主窗口的文件能力            |

实现时优先复用成熟、小型依赖或现有 Electron/Node 能力。若聚焦回归测试证明本节契约无法保证正确性，应先在 Tracker 中记录失败场景、根因和最小修正方案，再调整架构。

---

# 第一部分：文档保存与异常恢复

## 5. 原子化文档保存

起始基线中的普通文档保存直接写入目标文件。批次 1 已改为尽可能安全的替换流程，避免程序崩溃、系统断电或磁盘错误导致原文件被截断；其余跨平台实体机验证和 watcher 正文比较仍按下列任务继续推进。

要求：

- [x] 在目标文件同目录创建唯一临时文件，避免跨文件系统移动。
- [x] 完整写入、同步并关闭临时文件后再替换目标文件。
- [x] 尽可能保留原文件权限等必要元数据。
- [x] 失败时不得提前删除或截断原文件。
- [x] 失败后清理本次创建的临时文件；清理失败只记录相关路径，不得覆盖原始错误。
- [x] 成功后更新 `savedContent`、修改标记、最近文件和状态提示。
- [ ] 保存失败时标签仍保持 modified 状态，并向用户显示包含文件名和可理解原因的错误。
- [x] 自动保存与手动保存统一调用同一安全写入服务。
- [x] 所有文档正文写入只能经过这一安全写入服务；调用方不得各自实现临时文件、替换或权限保留逻辑。服务的文件系统操作应可注入，以便逐阶段制造故障并验证结果。
- [x] 写入前比较待保存字节与当前保存基线；内容确实未变化时不重写磁盘，避免无意义地改变 mtime 或触发 watcher。比较失败时进入正常安全写入，不能把“不确定”当作“相同”。
- [x] 成功保存后记录本次写入的预期正文或字节；watcher 收到事件时读取稳定磁盘内容，并以该内容比较结果判断是否为应用自身保存。
- [x] 明确定义“已提交”的边界：临时文件已完整写入并同步、关闭且成功替换目标。
- [ ] 明确验证 Linux、Windows、macOS 的替换语义，不能假定三个平台的 `rename` 行为完全一致。
- [ ] 在 Linux 开发阶段，为替换失败、文件被占用、目标已存在、大小写仅变化的重命名等结果建立可测试的错误/结果模型；后续 Windows/macOS 实机验证据此确认真实语义，不将 Linux `rename` 行为编码为通用规则。
- [x] 原子替换产生的目标和临时文件事件必须识别为应用自身保存，不能误报为外部冲突。

必须增加故障测试：

- 写入临时文件失败时原文件内容不变。
- 替换目标失败时原文件仍可读取，标签仍为未保存状态。
- 成功保存后临时文件不存在。
- 保存已有文件时文件权限没有无故变化。
- CRLF/LF 行尾策略继续遵守当前文档状态。
- 待保存内容与磁盘基线相同时不产生替换、mtime 变化或外部冲突事件。
- 应用自身保存事件中的磁盘正文与预期保存正文一致时不触发冲突；时间窗口内出现不同正文时仍必须进入外部变化流程。

## 6. 未保存内容恢复

现有 session 主要保存工作区和已打开文件路径，不能恢复无路径的新文档和已修改但尚未写入磁盘的内容。0.2.0 增加轻量恢复机制。

要求：

- [x] 在应用数据目录中使用独立的 recovery 数据，不把大量正文写进 `config.toml`。
- [x] 为无路径文档和 modified 文档保存恢复快照。
- [x] 快照至少包含稳定 ID、原文件路径（如有）、标题、正文、编码/行尾相关信息、修改时间和当前编辑模式。
- [x] 使用防抖写入，避免每个按键都同步写磁盘。
- [x] recovery 文件本身采用安全写入。
- [x] recovery 格式包含简单 schema 版本，并对必要字段和单快照大小做运行时校验；损坏、超限或未知版本的快照不得使应用启动失败。
- [x] 无法解析的快照记录不含正文的诊断信息并跳过；错误提示和日志不得泄露完整正文。
- [x] recovery 目录视为应用私有数据，不加入本地资源协议受控根；renderer 只通过恢复候选、恢复和放弃所需的窄 IPC 使用该数据；在平台支持时使用仅当前用户可读写的权限。
- [x] 文档成功保存且内容与磁盘一致后删除对应快照。
- [x] 用户主动“不保存”关闭文档后删除对应快照。
- [x] 正常退出且所有文档已妥善处理后清理无用快照。
- [x] 异常退出后再次启动，直接打开可恢复内容，并以文档级警示横幅提供保存、另存为或放弃恢复操作。
- [x] 恢复不能静默覆盖磁盘上更新的文件；原文件未变化时可保存此版本，磁盘版本已变化或原文件不可用时保留恢复内容但仅允许另存为。
- [x] 恢复无路径文档时继续作为 Untitled 文档打开，不擅自创建磁盘文件。

验收标准：

- 新建未命名文档并输入内容，模拟异常终止后可以恢复。
- 修改已有文档但未保存，模拟异常终止后可以恢复。
- 正常保存后不再重复提示旧快照。
- 用户明确放弃恢复后，下一次启动不再提示同一快照。
- 构造损坏、超限和未知 schema 快照时应用仍能启动，并只处理可验证的快照。

---

# 第二部分：外部文件变化与工作区一致性

## 7. 打开文件的监控范围

当前监控主要依赖工作区 watcher，独立打开且位于工作区之外的文件也必须被监控。

当前基线：工作区 watcher 报告目录结构，打开文档拥有独立的内容 watcher；稳定正文、预期保存正文和独立文件状态由 renderer 决策。应用自身保存使用短时抑制配合正文比较，不能仅依赖时间窗口。

要求：

- [x] 工作区 watcher 仅处理目录结构事件；每个已打开文档使用独立内容 watcher，避免同一内容事件重复监听或重建文件树。
- [x] 工作区之外的已打开文件建立必要的文件或父目录监听。
- [x] 标签关闭、文件另存、工作区切换和应用退出时正确释放监听资源。
- [x] 同一文件被多个入口打开时不创建重复 watcher。
- [x] 应用自身保存引发的文件变化不能被误报为外部冲突。
- [x] 不再把固定 1.5 秒 `ignoredChanges` 时间窗作为唯一判断。可保留短时去抖，但任何事件最终以稳定读取到的磁盘正文与当前标签正文/保存基线比较为准。
- [ ] 规范化路径身份并验证大小写敏感/不敏感文件系统、符号链接和同一文件不同路径表示的行为。
- [ ] 文件身份比较统一经过集中的路径规范化 helper，并覆盖 Windows 盘符、大小写不敏感卷及 macOS 默认卷可能出现的路径别名。
- [x] 正确合并编辑器、资源管理器和同步软件可能产生的短时间连续 `add` / `change` / `unlink` 事件，避免重复横幅或错误自动重载。
- [x] 文件内容 watcher 使用 chokidar `awaitWriteFinish` 或等价的稳定等待；目录树 watcher 不因读取正文的稳定等待而无故延迟节点显示。
- [x] 每个 watcher 都有明确的 disposed/cleanup 状态；关闭、另存、重绑或切换工作区后，已开始但尚未完成的异步读取不得再更新标签状态。
- [x] mtime、大小等元数据只用于减少不必要读取，不能作为内容相同的最终依据；磁盘正文相同时忽略事件，正文不同时进入自动重载或冲突流程。
- [x] watcher 以规范路径标识监听目标，并在回调和异步读取完成时检查当前绑定及 disposed 状态；已失效的结果直接丢弃。

批次 3-5 已完成的文件状态边界：

- [x] 外部删除或权限不可读进入独立持久状态，暂停自动保存并保留内存正文；不会静默重建原文件。
- [x] 文件重新出现时不自动替换内存正文，并额外发送工作区结构事件刷新 sidebar。
- [x] 删除状态下明确确认重建后，复制首次进入不可访问状态时冻结的正文；自动保存开启时取当刻编辑器正文，关闭时取最后一次成功保存的正文。
- [x] 重建成功显示 5 秒非驻留通知；复制失败不回滚已完成的重建。

工作区级资源边界已在 Tracker 批次 5.1 实施，Linux 主要场景已手测：Files & Session 提供持久化的 7–12 最大读取深度滑块，目录树和工作区 watcher 使用同一边界，watcher 资源错误会收敛为一次可理解的降级提示。

## 8. 外部修改冲突状态

当磁盘文件变化且当前标签存在未保存内容时，不能只显示会自动消失的状态栏消息，也不能允许自动保存随后覆盖外部内容。

当前基线：冲突标签已有持续可见的 `!` 标记；活动标签在编辑区顶部显示不抢焦点的持久横幅；可选择“忽略当前标签修改并重载”或“忽略外部更改”；冲突未处理及忽略外部更改后会暂停自动保存；无本地修改的已有文件继续自动重载。sidebar 新建或重命名不会被 Untitled 标签阻止，而是由同一路径映射进入该冲突流程。

批次 4-5 已完成文件级 modified/deleted/reappeared/unreadable 决策流程：稳定磁盘正文快照驱动重载，modified 横幅提供重载、另存当前内容、忽略外部更改和明确覆盖；deleted/unreadable 横幅暂停自动保存并提供与状态匹配的保护动作；reappeared 不静默覆盖内存。忽略后的保存仍需二次确认，重复外部变化会使旧覆盖确认失效。持久横幅统一使用 warning SVG；重建成功通知使用蓝色 `notification.svg` 和 5 秒计时。目录级路径状态已由批次 6 完成。

### 8.1 文档级持久提示

将当前外部修改横幅的定位、宽度、主题色、图标/状态色、文案和动作布局抽取为可复用的**文档级持久提示**视觉原语，例如 `document-notice`。它仅服务于当前活动标签的、需要用户明确处置的文档状态，固定在编辑区顶部且不抢占编辑焦点。

不得将其实现为任意模块可随意调用的全局通知通道：具体提示的可见性、自动保存暂停、按钮动作和清理时机必须继续由文件/标签领域状态驱动。短暂成功消息与普通错误仍使用现有状态栏消息，不与持久提示混用。

`modified`、`deleted`、权限不可读等状态可以复用该外观，但各自必须拥有独立的文案、动作集合和状态转换，不能只更换一行提示文字。

### 8.2 对话框卡片与受限拖动

`document-notice` 不与确认对话框合并为同一种 UI 组件：前者是编辑区内、不抢焦点的文档状态提示，后者是带遮罩、焦点和明确操作结果的模态决策界面。两者可以共享颜色、边框、圆角、阴影、标题/动作间距等设计 token，但不得共享可见性、状态转换或动作分发逻辑。

将对话框的共享外观和可选交互收敛为 `dialog-card` 约定：确认框、未保存变更框和未来的应用内决策框复用 card/header/content/footer 结构；各对话框仍各自拥有 Promise resolver、焦点策略、Escape 行为和按钮语义。不要将其扩展为任意模块可调用的全局 modal 服务。

当前未保存变更框已支持在窗口范围内拖动、关闭后恢复居中且不可调整尺寸；设置框另有尺寸调整与持久化需求。0.2.0/后续 renderer 拆分时，应将两者重复的边界计算抽为小型受限拖动 helper，例如 `setupBoundedCardDrag({ card, container, handle, enabled })`：

- [ ] helper 只处理指针拖动、窗口边界收敛、窗口 resize 后重新收敛和 listener cleanup；不处理尺寸调整、位置持久化、焦点或业务动作。
- [ ] 拖动是否启用必须由具体对话框声明；设置框、未保存变更框和普通确认框不得因复用 helper 而获得相同的尺寸调整或持久化语义。
- [ ] 为可拖动对话框测试标题栏拖动、窗口边界、关闭后位置复位和“尺寸不变”；为不可拖动对话框测试其默认居中与不可拖动状态。
- [ ] 只有出现第三类具有相同模态语义的可拖动卡片时，再评估是否将 `dialog-card` 升级为独立 DOM/controller 模块；在此之前保持轻量 helper，避免形成新的全局 UI 框架。

引入持久的冲突状态，例如：

```ts
type ExternalConflict = null | {
  kind: 'modified';
  detectedAt: number;
  diskMtime?: number;
};
```

后续补完：

- [x] “忽略外部更改”后的手动保存必须再次明确确认覆盖，不能仅依赖用户点击保存来推断意图。
- [x] 外部修改补充“另存当前内容”和“明确覆盖磁盘版本”，并与现有两个横幅动作形成完整流程。
- [x] 外部删除立即进入 `deleted` 持久状态并暂停自动保存；保留内存正文、编辑模式、光标和撤销历史，绝不因 watcher 事件或自动保存静默重新创建原文件。
- [x] 有未保存内容的外部删除提示提供“另存当前内容”“重新创建原文件”“关闭文档”；重新创建原文件须经明确确认。
- [x] 无未保存内容的外部删除提示提供“保留为未命名文档”“关闭文档”和经确认的“重新创建原文件”；不使用“重载”，因为磁盘目标不存在。
- [x] 文件在删除提示期间重新出现时，不静默覆盖内存内容；转入明确的“文件重新出现/外部变化”状态，由用户选择重载、另存或写回。
- [x] 对删除、权限变化和磁盘文件不可读分别建模，不能统一当作普通修改。
- [x] 冲突状态保存检测时间、稳定磁盘正文版本和递增版本号。
- [ ] 应用重启与 recovery 恢复后仍能识别尚未解决的外部文件状态；当前 session 不持久化运行时 `externalFileState`，这是后续恢复设计事项。
- [x] 外部文件在横幅显示期间再次变化时更新磁盘版本，并保证“重载”读取的是最新稳定内容。
- [ ] 自动重载和手动重载尽可能保留或明确重置光标、选区、滚动位置、编辑模式和撤销状态。
- [x] 正常打开路径会去重，单一路径只保留一个标签映射；同一路径的相关标签更新时共享同一稳定磁盘事实。
- [x] 解决冲突后清除标记、忽略状态和旧事件记录，并重建正确的 `savedContent` 与保存基线。

如果磁盘文件变化而标签没有本地修改，可以继续自动重新载入，但应确保光标、滚动位置和用户提示尽可能合理。

## 9. 目录重命名与删除

批次 5 已覆盖单文件外部删除及重新出现；本节只处理目录级路径一致性和工作区根目录失效，不重复定义文件级保护状态。

- [ ] 重命名目录时，更新该目录下所有已打开子文件的 `filePath`、`baseDir`、标签提示和最近文件记录。
- [ ] 更新后的相对图片资源仍能正常显示。
- [x] 删除已打开文件时，标签进入“外部删除”状态，不能在用户不知情的情况下通过自动保存重新创建文件（已由批次 5 完成；本节后续项仍是目录级删除影响）。
- [ ] 删除包含已打开文件的目录时，对所有受影响标签执行同样处理。
- [ ] 文件另存后重新绑定正确 watcher，并解除旧路径 watcher。
- [ ] 识别资源管理器重命名所产生的旧路径 `unlink` 与新路径 `add` 事件对，避免把应用内重命名误报为删除和外部新建两个冲突。
- [ ] 工作区切换时重新计算 Untitled 默认目标路径；旧工作区的迟到 watcher 事件不得绑定到新工作区标签。
- [ ] 应用发起重命名、删除或另存时，先暂时解绑受影响 watcher，操作完成后更新标签路径并重新绑定；重绑后读取当前磁盘事实，不能用固定时间窗口吞掉同时发生的外部操作。
- [ ] 工作区根目录在应用外被删除后，运行中的 sidebar、标题、session 路径和 watcher 一致地复位为空工作区；启动恢复时也拒绝恢复不存在的路径。

---

# 第三部分：Electron 安全边界

## 10. 安全原则

Vditor-Electron 会打开用户提供的 Markdown，其中可能包含 HTML、链接和远程资源。渲染器同时能够通过 preload 调用本地文件功能，因此 0.2.0 必须缩小“渲染内容 → Electron 权限 → 本地文件系统”的组合风险。

安全加固不能只修改一个开关，必须按入口逐项检查。

## 11. 导航与外部链接

- [ ] 为 `window.open`、`will-navigate` 和 preload 暴露的 `openExternal` 使用同一套 URL 校验函数。
- [ ] 只允许明确支持的 `https:`、`http:` 和必要的 `mailto:`。
- [ ] 拒绝 `file:`、`javascript:`、`data:`、自定义应用协议和无法解析的 URL。
- [ ] 不允许 Markdown 中的链接触发应用内部导航到任意网页。
- [ ] 为 URL 校验函数增加单元测试，包括大小写、空白、编码和恶意 scheme 边界情况。

## 12. IPC 调用方校验与参数校验

- [ ] 为所有具有文件、窗口、导出、设置和外部链接权限的 IPC handler 校验 `event.senderFrame` / 来源 URL。
- [ ] 只接受来自本应用受信任 renderer 页面的调用。
- [ ] 抽取一个小型受信任主 frame 校验 helper，由所有特权 handler 复用。
- [ ] 来源校验使用解析后的精确应用 origin、页面路径和主 frame 身份，不使用 `startsWith`、模糊 host 匹配或仅检查 `event.sender` 是否存在。
- [ ] 对文件写入、删除、重命名、目录创建、导出和外部链接等高风险入口的路径、枚举、数字范围和对象结构进行运行时校验，不能只依赖 TypeScript 类型。
- [ ] 先完成来源和参数校验，再访问文件系统、显示系统对话框或执行其他副作用。
- [ ] preload 不暴露底层 `ipcRenderer`，继续保持窄 API。
- [ ] 对删除、重命名、创建目录、写文件等高风险操作采用明确参数结构并返回可处理的错误。
- [ ] 对 renderer 返回稳定、可本地化且不泄露绝对敏感路径或内部堆栈的领域错误；详细诊断只进入受控主进程日志。
- [ ] 把 IPC channel、输入和返回结构集中定义，避免 main、preload、renderer 三处漂移。
- [ ] 创建和重命名项目时，将平台不接受的名称、权限/占用和已存在等失败转换为可本地化的领域错误；不得把 Linux 上可创建的文件名视为所有平台都可接受。

## 13. 本地资源协议与文件能力范围

当前本地图片协议不能继续把任意绝对路径视为可读取资源。

目标行为：

- [ ] 仅允许读取当前已打开文档、其授权资源目录或已打开工作区范围内的资源。
- [ ] 本地资源协议维护受控根目录集合：当前工作区根，以及工作区外已打开文档各自的目录。普通打开、保存、重命名和删除继续使用各自的窄 IPC 与参数校验。
- [ ] 先拒绝无效百分号编码、NUL、反斜杠混淆、绝对路径注入和 `..`，再把 URL 路径转换为本地路径并通过规范化、realpath 和路径边界确认其位于受控根内。
- [ ] 目录包含关系使用路径边界和规范化后的比较，不使用简单字符串前缀；比较规则必须考虑 Windows 盘符与大小写不敏感文件系统。
- [ ] TOML 配置、Chromium user data 和 recovery 目录不得被注册为资源根；协议仅响应已授权的编辑器本地资源请求。
- [ ] 不允许通过协议探测任意系统文件是否存在；对未授权与不存在目标返回外部不可区分的中性失败，内部日志仍保留拒绝原因。
- [ ] 文档关闭或工作区关闭后撤销不再需要的访问能力。
- [ ] 继续支持 Markdown 相对图片、粘贴图片目录和现有本地预览。
- [ ] 路径授权通过后仍按资源类型决定响应：仅内联应用实际需要的图片/字体等类型，设置准确的 `Content-Type` 和 `X-Content-Type-Options: nosniff`；HTML、JavaScript、XML 和未知类型不得作为可执行内容内联。
- [ ] SVG 只有在准确 MIME 和真实 Electron 测试证明其作为图片不会执行脚本或导航时才允许响应；否则拒绝并记录为兼容限制。
- [ ] 为合法路径、越界路径、编码绕过、反斜杠、符号链接越界、不存在路径、私有目录和文档/工作区关闭后的授权撤销增加测试；Windows junction 由纯路径测试覆盖，实体机状态如实记录。

根目录在打开/关闭文档和工作区时增删；扩展名和 MIME 只决定已授权资源如何响应，不能替代路径授权。

## 14. Renderer sandbox、CSP 与 HTML 清理

- [ ] 在测试现有 Vditor、preload 和本地协议兼容性的前提下，尝试启用 BrowserWindow renderer sandbox。
- [ ] 处理当前 Electron DevTools 对 CSP 中 `'unsafe-eval'` 的安全警告：检查 `'unsafe-eval'` 和 `'unsafe-inline'` 的真实依赖，能够移除的必须移除；不能移除的要通过最小复现实验在安全文档中说明具体原因、受影响的 Vditor 能力和限制范围。打包后不再显示警告不等于风险已消失。
- [ ] 保持 `contextIsolation: true` 和 `nodeIntegration: false`。
- [ ] Markdown sanitize 默认保持开启。
- [ ] 如果继续允许用户关闭 sanitize，设置页面必须明确显示风险说明；在安全边界尚未完成前，不应让未清理 HTML 与宽泛文件能力同时存在。
- [ ] 禁止用自动添加 `--no-sandbox` 作为一般性兼容方案；如 Linux 特殊环境确实需要回退，应明确记录触发条件和风险。

如果某项因为 Vditor 或 Electron 限制无法在 0.2.0 完成，agent 必须提供复现实验、风险说明和最小替代措施，不能静默跳过。

---

# 第四部分：已确认缺陷修复

## 15. macOS 窗口关闭确认状态

当前关闭确认状态是进程级变量。一次确认关闭后，在 macOS `activate` 重新创建窗口时可能没有复位，从而让新窗口的未保存文档绕过确认。

要求：

- [ ] 每次创建新 BrowserWindow 时重新初始化关闭确认状态，或改为按窗口保存状态。
- [ ] 一个窗口的确认不能影响以后创建的窗口。
- [ ] 正常关闭、取消关闭、保存后关闭和“不保存”关闭均保持正确行为。
- [ ] 增加可在非 macOS 环境执行的状态单元测试；条件允许时增加 macOS 集成验证。

## 16. 设置保存不应无条件重建全部编辑器

当前保存任意设置后会重建所有 Vditor 实例，可能清除撤销栈、选区和滚动位置。

要求：

- [ ] 对设置进行分类：可热更新、只影响布局/展示、确实需要重建编辑器。
- [ ] 主题、字体、缩放、工具栏显示、侧边栏和窗口布局优先热更新。
- [ ] 只有 Vditor 初始化后无法安全改变的选项才触发重建。
- [ ] 如必须重建，尽可能保存并恢复内容、模式、光标、选区和滚动位置，并明确撤销历史限制。
- [ ] 增加测试确认只修改外观或工具栏显示后，用户仍可撤销之前的编辑。

## 17. HTML/PDF 导出中的本地图片来源

- [ ] HTML 和 PDF 导出统一使用原始 Markdown 图片来源，不输出仅在应用内部有效的 `local-file://` URL。
- [ ] 复用现有 `withOriginalImageSources` 或等价的集中能力，不创建第二套转换规则。
- [ ] 导出开始时从当前 Markdown/渲染结果建立一次性内容快照；HTML 恢复可移植的原始/相对图片来源，PDF 优先把需要的本地图片转换为 data URL 或其他不依赖主窗口权限的自包含来源。
- [ ] PDF 隐藏窗口使用隔离配置：无业务 preload、`nodeIntegration: false`、`contextIsolation: true`、可行时启用 sandbox，禁止新窗口和非预期导航；不得继承主编辑窗口的宽泛能力。
- [ ] HTML/PDF 产物不得包含 `app://`、`local-file://` 或其他仅应用内部有效的地址。
- [ ] 明确导出是否跟随当前内容主题；如果 0.2.0 不实现主题化导出，至少保持输出稳定并在文档中说明。
- [ ] 增加包含相对图片的导出测试，断言结果中不存在 `local-file://`。

---

# 第五部分：代码组织与可维护性

## 18. 渲染层修改原则

`src/renderer/app.js` 已承担标签、Vditor 生命周期、菜单、设置、工作区、主题、导出和文件状态等大量职责。0.2.0 不要求一次性重写。

最低要求：

- [ ] Vditor DOM 查询继续通过 adapter 完成。

现状约束（不属于 0.2.0 新功能目标）：0.1.5 在无文档标签页时保留一个 `state.toolbarPreview` Vditor 实例，仅用于生成与默认编辑模式匹配的共享 toolbar。该预览不属于文档标签、不参与文件保存或会话持久化；其按钮必须保持 disabled 灰化，`View > Layout > Show Toolbar` 仍可控制显隐。打开文档或设置变化时应正确销毁并重建预览，后续 renderer 重构不得把它误当成真实文档实例。

可选但非发布阻塞：逐步把 renderer 新模块写成 TypeScript，并建立共享 IPC 类型。不得以此为理由对整个 `app.js` 做高风险全量迁移。

## 19. 应用标识、依赖与构建一致性

- [ ] 统一 `package.json`、electron-builder、desktop file、AppStream metadata 中的应用 ID 和名称。
- [ ] 明确项目支持的 Node.js 版本，并在 `package.json` engines 与文档中保持一致。
- [ ] 解决当前安装环境可能出现的依赖 engine 警告。
- [ ] Electron 版本、lockfile 和依赖执行许可配置保持一致并可复现。
- [ ] 继续保留 Linux 发布脚本中对 AppImage 工具和 runtime 的版本固定及 SHA-256 校验。

---

# 第六部分：测试、发布准备与文档

## 20. 自动化测试要求

### 20.1 单元测试

至少覆盖：

- 原子保存成功、无变化跳过写入及各阶段失败。
- recovery 快照创建、清理、恢复、简单 schema/大小限制和损坏跳过。
- 外部修改/删除冲突状态转换。
- watcher 稳定等待、正文相同比较、cleanup/disposed 和迟到异步结果失效。
- 目录重命名对子文件标签路径的更新。
- URL scheme allowlist。
- IPC 主 frame/子 frame 来源与高风险参数边界校验。
- 本地资源受控根、URL/本地路径转换、realpath、私有目录及越界拒绝。
- 本地资源响应类型、`nosniff`、未知/主动内容拒绝和 SVG 限制。
- macOS 新窗口关闭确认状态复位。
- 导出内容不包含内部 `local-file://` 地址。

### 20.2 E2E 测试

至少覆盖：

- 无文档标签时默认模式 toolbar 预览的显示、disabled 灰化，以及布局菜单显隐切换。
- 设置外观和工具栏显示后撤销历史仍可使用。
- 外部修改冲突时自动保存被暂停。
- 外部修改冲突横幅的重载、另存、忽略后确认覆盖和显式覆盖动作，以及重复磁盘变化使旧确认失效。
- 外部删除后不会静默重新创建文件；重新出现不会静默覆盖内存正文；不可读状态会暂停自动保存。
- 重建前正文备份到系统剪贴板，自动保存开启/关闭分别冻结正确的正文基线，复制成功显示 5 秒通知，复制失败不回滚重建。
- 异常退出后的未命名文档恢复。
- watcher 在另存、标签关闭和工作区切换后的迟到读取不会污染新状态。
- 本地资源协议无法通过编码或符号链接越界，允许的资源类型不能取得脚本执行或应用导航能力。
- PDF 导出窗口保持隔离，HTML/PDF 结果不包含应用内部协议。

E2E 测试必须等待明确的 UI 状态或数据状态，不使用大段固定 sleep 掩盖竞态。

## 21. 本地验证与手动发布

0.2.0 不引入 GitHub Actions 云端自动构建或交付产物。发布前在受控的本地环境至少执行：

1. 依赖安装。
2. Prettier 检查。
3. ESLint。
4. TypeScript 类型检查。
5. Vditor 固定版本检查。
6. 单元测试。
7. 构建。
8. 在兼容的本地 Linux 环境执行 Electron E2E，并手动生成候选安装包。

要求：

- [ ] 本地验证使用项目明确支持的 Node.js 版本。
- [ ] 本地依赖缓存只能加速安装，不能掩盖 lockfile 问题。
- [ ] E2E 环境问题与测试断言失败在日志中可以区分。
- [ ] release 前所有必需检查必须通过。
- [ ] dev分支最后merge main的提交之前，确保项目各处软件版本号以改为`dev-<版本号>`开发分支显示的版本号。

手动发布流程示例：

```text

1. 完成本地开发：git add . && git commit -m "feat: xxx"

2. dev分支远端推送：git push origin dev-1.0.2 （推送分支）

3. 手动：GitHub网页 创建 PR，合并到 main

4. 本地手动同步合并提交：git fetch origin && git switch main && git pull origin main

5. 本地执行完整检查并生成候选包，完成安装包冒烟验证

6. 本地手动创建标签并推送：git tag v1.0.2 && git push origin v1.0.2

7. GitHub 网页手动创建 Release 草稿，上传已验证的本地产物并填写更新日志

8. 点击 Publish release
```

## 22. 文档更新

- [x] 更新 README 中的当前文件状态、恢复和跨平台验证说明。
- [x] 更新设置文件字段说明：批次 5.1 新增 `files.workspaceReadDepth`（7–12，默认 7）；旧配置缺失该字段时使用默认值。
- [x] 更新 `docs/01-CODE-STRUCTURE.md` 的当前代码边界、Bridge API、文件状态和测试覆盖。
- [x] 更新 `docs/13-0.2.0-EXECUTION-TRACKER.md` 的批次 5 结果和批次 5.1/6 范围。
- [x] 扩充 `docs/00-ISSUES.md`，记录未完成的限制和后续版本候选项。
- [ ] 增加简短安全说明：Markdown HTML、外部链接、本地资源协议和 sanitize 的边界。
- [x] 编写 0.2.0 changelog，区分新功能、修复、安全改进和已知限制。

---

# 第七部分：实施顺序与提交建议

## 23. 推荐阶段

### 阶段 A：基线检查

1. 完成基线检查。

### 阶段 B：保存与文件冲突

1. 统一安全写入服务和无变化跳过。
2. recovery 快照。
3. 工作区外文件 watcher、稳定等待与正文比较。
4. 外部修改/删除冲突 UI。
5. 文件级删除、重新出现和不可读状态（已完成）。
6. 工作区目录读取与监听深度边界（批次 5.1，待实施）。
7. 目录重命名、删除和另存后的标签及 watcher 修复。

### 阶段 C：安全加固

1. 统一 URL 校验。
2. 统一 IPC 主 frame 校验 helper，并完成高风险参数校验。
3. 建立本地资源受控根，限制协议路径并收紧资源响应类型。
4. 验证 renderer sandbox 和 CSP。
5. 调整 sanitize 风险展示。

### 阶段 D：缺陷、导出与设置热更新

1. 修复 macOS 关闭确认状态。
2. 避免设置保存无条件重建全部编辑器。
3. 修复 HTML/PDF 导出图片来源并隔离 PDF 隐藏窗口。
4. 统一应用 ID、Node 和依赖配置。

### 阶段 E：发布准备

1. 补齐单元测试和 E2E。
2. 执行本地完整检查。
3. 核对发布门槛并记录平台限制。
4. 更新 README、升级文档、问题清单和 changelog。
5. 生成候选安装包并进行 Linux 实机冒烟测试；条件允许时验证 Windows/macOS。

## 24. 提交原则

- 每个提交只解决一个可描述的问题，避免把 UI、保存、安全和格式化混在同一提交。
- 重构提交和行为修改提交尽量分开。
- 每个阶段结束前运行与该阶段相关的测试。
- 不使用跳过测试、关闭 lint 或放宽类型来换取表面通过。
- 不批量格式化与任务无关的文件。
- 不修改 Vditor vendored 代码来绕过 adapter，除非先证明没有可维护的外部方案。
- commit message 应说明行为变化，例如：

```text
fix(files): prevent autosave from overwriting external changes
fix(save): replace documents through atomic writes
security(ipc): validate privileged renderer requests
```

---

# 第八部分：0.2.0 发布门槛

以下条件全部满足后，才可以把版本标记为 0.2.0：

## 25. 功能门槛

- [x] 原子保存和异常恢复通过故障测试。
- [x] 外部文件冲突不会被自动保存静默覆盖。
- [ ] 目录重命名、删除和另存后的标签路径与 watcher 正确。
- [ ] macOS 新窗口不会继承旧窗口的关闭确认状态。
- [ ] HTML/PDF 导出不包含无法在外部打开的内部资源 URL。

## 26. 安全门槛

- [ ] 外部导航和 `shell.openExternal` 统一执行 scheme allowlist。
- [ ] 特权 IPC 统一校验受信任主 frame，并对高风险输入执行运行时校验；子 frame 不能调用。
- [ ] 本地资源协议只能读取当前受控根内的资源，不能读取授权范围外或应用私有目录中的任意文件。
- [ ] 本地资源仅以内联 allowlist 和安全响应头返回；HTML、JavaScript 和未知类型不可执行，SVG 按已验证策略允许或明确拒绝。
- [ ] 文档和工作区关闭后对应资源根被撤销；PDF 导出窗口不继承主窗口 preload 或文件能力。
- [ ] `contextIsolation` 保持开启，`nodeIntegration` 保持关闭。
- [ ] sandbox、CSP 和 sanitize 的最终状态经过验证并有文档说明。

## 27. 质量门槛

- [x] Prettier、ESLint、TypeScript 类型检查全部通过。
- [x] Vditor 版本固定检查通过。
- [x] 单元测试全部通过。
- [x] 项目构建通过。
- [ ] Electron E2E 在兼容环境全部通过。（2026-08-26 全量运行 89 个用例，其中 2 个既有 UI 用例超时；随后精确重跑均通过，具体结果记录在 Tracker，仍需后续复核。）
- [x] README、changelog 和已知问题与实际版本一致。
- [ ] Linux 候选安装包完成打开、编辑、保存、恢复和导出冒烟测试。
- [ ] 已记录 Windows/macOS 实机验证、打包和发布尚未完成；不得因 Linux 通过而宣称三平台已支持。

---

# 第九部分：0.2.0 明确不做的事项

以下任务不属于 0.2.0，除非为了完成发布门槛必须做最小调整：

- Vditor 版本升级。
- React、Vue 或其他前端框架迁移。
- `app.js` 一次性全量 TypeScript 重写。
- 插件系统。
- 完整命令面板或复杂快捷键自定义系统。
- 为没有明确需求的格式或平台增加大规模导入导出功能。
- 仅为了视觉变化重做整套主题。

这些内容如果在开发过程中被发现有价值，应记录到 `docs/00-ISSUES.md` 或 `docs/30-WISHLIST.md`，而不是临时扩张 0.2.0。

## 28. 最终开发汇报格式

完成后请提供一份简明但可核验的汇报，包括：

1. 实际完成的任务及对应 commit。
2. 与本文档相比发生的范围调整及原因。
3. 新增或迁移的设置字段。
4. 保存、恢复、冲突和安全模型的最终实现说明。
5. 自动化测试、构建、E2E 和实机测试结果。
6. 未完成事项、已知限制及建议进入 0.2.1 的内容。
7. 需要人工重点体验的交互路径。

不要只汇报“已完成”或列出修改文件；必须说明用户可观察到的行为变化、风险控制结果和仍然存在的限制。
